"use client";

// This component is no longer used. Its functionality has been merged into
// the new LookupTool component located in `maintenance-scheduler.tsx`.
export function TechnicalDataViewer() {
  return null;
}
